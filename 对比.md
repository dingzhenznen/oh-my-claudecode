
  不会有直接冲突，但会有角色重叠和优先级混乱。

  两者的本质差异

  ┌──────────┬────────────────────────────────────┬──────────────────────────────────────┐
  │   维度   │       OMC (oh-my-claudecode)       │            agency-agents             │
  ├──────────┼────────────────────────────────────┼──────────────────────────────────────┤
  │ 安装位置 │ ~/.claude/（全局）                 │ ~/.claude/agents/（agent 子目录）    │
  ├──────────┼────────────────────────────────────┼──────────────────────────────────────┤
  │ 运行层级 │ 编排层：管理工作流、状态、任务队列 │ 角色层：定义专门 agent 的人格和能力  │
  ├──────────┼────────────────────────────────────┼──────────────────────────────────────┤
  │ 触发方式 │ 关键词、hooks、slash commands      │ 用户显式激活（"Use the X agent..."） │
  ├──────────┼────────────────────────────────────┼──────────────────────────────────────┤
  │ 功能     │ workflow、状态机、团队协作         │ 单个 agent 的专业 prompt             │
  └──────────┴────────────────────────────────────┴──────────────────────────────────────┘

  实际关系：互补而非竞争

  OMC (编排引擎)
      ├── 调度 → OMC 自带 agents (executor/planner/debugger...)
      │
      └── 调度 → agency-agents 安装的 agents
                 (engineering-code-reviewer / engineering-senior-developer...)

  agency-agents 里的 agent 文件（如 engineering-code-reviewer.md）安装到 ~/.claude/agents/ 后，本质上就是 Claude Code 的
  sub-agents，OMC 可以调用它们。

  潜在的摩擦点（不是冲突，但要注意）

  1. 角色重叠：OMC 有 code-reviewer，agency-agents 也有 engineering-code-reviewer。两个存在时，Claude
  要靠名称判断用哪个，不会出错但可能选择不一致。
  2. 指令竞争：如果 agency-agents 的某个 agent 内部有与 OMC CLAUDE.md 矛盾的指令（比如不同的 commit 格式），执行时以
  CLAUDE.md 优先（用户指令最高）。
  3. agents-orchestrator 的特殊情况：你提到的那个 md 文件在 agency-agents/specialized/ 目录，不一定被安装进
  ~/.claude/agents/。如果没安装，Claude Code 根本找不到它。

  检查是否安装

  ls ~/.claude/agents/ | grep orchestrator

  如果没有这个文件，那段触发指令会失效，OMC 会接管处理。

  结论

  同时使用完全可行，OMC 负责编排，agency-agents 提供专业角色，两者分工明确。 唯一需要注意的是：避免同名 agent
  造成的选择混乱，以及确认 agency-agents 确实安装到了 ~/.claude/agents/。

✻ Crunched for 41s

❯ 如果选一个，我应该优先使用哪个                                                                                            

⏺ 优先用 OMC。

  原因

  agency-agents 是"角色库"，它只提供 agent 的人格定义（prompt 模板）。你需要自己决定用哪个 agent、怎么组合、任务完成没有。

  OMC 是"工作流引擎"，它解决的是更难的问题：

  - 任务拆分、并行执行、状态追踪
  - 自动选择合适的 agent 和 model
  - 验证完成、失败重试、结果审查
  - 全局记忆、项目上下文持久化

  两种典型场景对比

  ┌────────────────┬───────────────────────────────────────────────────────────────┬────────────────────────────────────┐
  │      场景      │                      只用 agency-agents                       │               用 OMC               │
  ├────────────────┼───────────────────────────────────────────────────────────────┼────────────────────────────────────┤
  │ 实现一个新功能 │ 你手动激活 senior-developer → 手动激活 code-reviewer →        │ /ralph 或 autopilot                │
  │                │ 手动验证                                                      │ 自动完成整个流程                   │
  ├────────────────┼───────────────────────────────────────────────────────────────┼────────────────────────────────────┤
  │ 调试报错       │ 激活 engineering-sre，给它贴错误                              │ OMC debugger agent 自动追踪根因    │
  ├────────────────┼───────────────────────────────────────────────────────────────┼────────────────────────────────────┤
  │ 并行任务       │ 你无法并行，只能顺序调用                                      │ OMC ultrawork / team 自动并行      │
  └────────────────┴───────────────────────────────────────────────────────────────┴────────────────────────────────────┘

  实用建议

  不必二选一——以 OMC 为主框架，按需从 agency-agents 借用特定角色：

  # 安装你需要的 agency-agents 角色
  cp agency-agents/engineering/engineering-code-reviewer.md ~/.claude/agents/

  # 然后在 OMC 中正常使用，它会自动找到这个 agent

  agency-agents 的价值在于它提供了大量现成的专