• 我先沿着“orchestrator 这个概念从哪进入系统”补查一下入口定义和约束点。

• Explored
  └ Search orchestrator|omcSystemPrompt|getSystemPrompt|createOmcOrchestratorHook|
           processOrchestratorPreTool|You are the relentless orchestrator|coordinator in
           index.ts

─ Worked for 19s ───────────────────────────────────────────────────────────────────────────

• 在这张时序图里，Orchestrator 不是先有一个单独类实例再出现的，它最早是以 系统角色 / system
  prompt 的形式被引入的。

  最早引入点

  - 真正的源头是 omcSystemPrompt，它直接把主代理定义成：
      - “the relentless orchestrator of a multi-agent development system”
  - 代码位置：src/agents/definitions.ts:289

  它怎么进入运行链路

  - src/index.ts 会导入这个 omcSystemPrompt，见 src/index.ts:17
  - 然后在创建主运行上下文时，把它拼进最终 system prompt：
      - let systemPrompt = appendSkininthegamebrosGuidance(omcSystemPrompt, 'system');
  - 代码位置：src/index.ts:283
  - 也就是说，从主会话初始化开始，系统就把当前这个“顶层 Claude”塑造成 orchestrator

  所以时序图里 O 从哪开始成立

  - 对应你图里的第一步，其实不是 U->>O
  - 更准确地说，在用户任务到来之前，系统已经先完成了：
      - omcSystemPrompt 定义 orchestrator 身份
      - src/index.ts 把这个 prompt 注入主代理上下文
  - 然后用户消息才进入这个“被设定为 orchestrator 的主代理”

  第二个关键引入点：行为约束

  - 仅有 prompt 还不够，系统还通过 hook 强化 orchestrator 行为
  - processOrchestratorPreTool() 会在工具执行前检查：
      - orchestrator 是否在违规直接改源码
      - 是否应该委派而不是自己实现
  - 代码位置：src/hooks/omc-orchestrator/index.ts:373
  - 对应的规则文本在：src/hooks/omc-orchestrator/constants.ts:65

  所以可以分成两层理解

  - System/Runtime -> O: 注入 omcSystemPrompt
  - Hooks -> O: 注入 orchestrator delegation rules
  - U -> O: 提出复杂任务

  一句话结论

  - Orchestrator 最早是在 src/agents/definitions.ts:289 通过 omcSystemPrompt 被引入的；
  - 然后在 src/index.ts:283 被装配进主代理上下文；
  - 再由 src/hooks/omc-orchestrator/index.ts:373 把这种身份约束成真实运行行为。

  如果你要，我可以继续帮你把你那张时序图补成“带初始化阶段的完整版本”。

  




监控收敛：src/team/runtime-v2.ts:1351




› sequenceDiagram
        participant U as 用户/上层目标
        participant O as Orchestrator
        participant A as Agent Registry
        participant T as Team Runtime
        participant R as Role Router
        participant S as Stage Router
        participant P as Task Router / Allocation
        participant M as Message Router
        participant W as Worker
        participant Mon as Monitor / Phase Controller

        U->>O: 提出复杂任务
        O->>O: 判断是否需要委派/并行
        O->>A: getAgentDefinitions()
        A-->>O: 返回可用代理表

        O->>O: Task(subagent_type=..., prompt=...)
        alt 普通子代理模式
            O->>W: 直接发起子代理任务
            W-->>O: 返回结果
        else Team Runtime 模式
            O->>T: startTeamV2() / 提交任务
            T->>T: 写入 tasks/*.json
            T->>S: buildResolvedRoutingSnapshot()
            S-->>T: 角色 primary/fallback 路由快照

            T->>R: routeTaskToRole(subject, description)
            R-->>T: 推断 canonical role

            T->>S: resolveRoleAssignment(role)
            S-->>T: provider + model + agent

            T->>P: allocateTasksToWorkers() / routeTasks()
            P-->>T: 选中目标 worker

            T->>M: routeMessage(team, worker, content)
            alt Claude native worker
                M-->>W: SendMessage 路径
            else MCP/CLI worker
                M-->>W: 写入 inbox JSONL
            end
            W-->>T: 更新 status / heartbeat / verdict


    对应源码锚点
    - orchestrator 总控提示：src/agents/definitions.ts:289
    - 背景 Task(...) 用法示例：src/features/magic-keywords.ts:101
    - team 启动：src/team/runtime-v2.ts:723
    - 任务角色推断：src/team/runtime-v2.ts:190
    - 角色路由解析：src/team/stage-router.ts:161
    - 路由快照构建：src/team/stage-router.ts:198
    - 任务分配：src/team/allocation-policy.ts:43
    - 能力路由：src/team/task-router.ts:35
    - 消息派发：src/team/message-router.ts:33
    - 监控收敛：src/team/runtime-v2.ts:1351
    - 阶段推断：src/team/phase-controller.ts:21

    一句话理解

    - Task(...) 不是直接“喊一个代理去干活”，而是先把任务转成 角色，再把角色解
      析成 具体执行配置，然后交给 team runtime 做 分配、投递、监控、回收。

    如果你要，我可以继续给你画第二张：

    - 组件关系图（agents / hooks / team / mcp 的静态结构）
    - 或 源码调用流程图（函数级 flowchart）
  根据 时序图 ，Orchestrator 在哪里开始被引入的





  src/index.ts 里那段 query({...}) 只是库模式示例
  - OMC 实际给 Claude Code 注入“总控规则”的主要方式，不是运行时执行这段代码
  - 在日常 OMC / 插件模式里，主要是通过安装到 Claude Code 环境里的 CLAUDE.md、agents、
    skills、hooks 来注入

  结论先说

  - omcSystemPrompt 主要是 库 API 层的系统提示模板
  - OMC 真正常用的“注入”方式是：
      - CLAUDE.md
      - plugin-provided agents
      - plugin-provided skills
      - hooks
  - 也就是：通过 Claude Code 的插件/配置生态注入，而不是每次运行时调用 query() 注入

  为什么这么说
  你前面看到的这段：

  - src/index.ts:257

  只是注释里的示例，说明：

  - 如果有人把 OMC 当成 TypeScript 库用
  - 那可以自己用 createOmcSession() + Claude Agent SDK 的 query() 来注入 systemPrompt

  但 OMC 仓库本身更常见的运行模式不是这个。

  OMC 实际常用注入链路
  从安装逻辑看，OMC 主要通过以下方式进入 Claude Code：

  - 1. 安装/更新 CLAUDE.md
      - 安装器会把 OMC 的 CLAUDE.md 合并写入 Claude 配置目录
      - 见 src/installer/index.ts:1779
      - 写入目标：~/.claude/CLAUDE.md
      - 见 src/installer/index.ts:1785
  - 2. plugin 模式提供 agents / skills / hooks
      - 当 plugin 已启用时，agents、skills、hooks 由 plugin 系统提供
      - 安装器甚至会跳过独立拷贝，因为这些资源“由 plugin system 管理”
      - 见：
          - src/installer/index.ts:1685
          - src/installer/index.ts:1702
          - src/installer/index.ts:1748
          - src/installer/index.ts:1761
  - 3. launch 时准备 Claude 运行环境
      - 启动链路里会把 companion CLAUDE.md 放到运行配置目录
      - 见 src/cli/launch.ts:94

  所以 OMC 在插件模式下怎么“注入”
  更准确地说是：

  - 不是把 omcSystemPrompt 直接塞进 Claude 内核
  - 而是通过 Claude Code 认可的配置/插件入口，把 OMC 的规则交给 Claude：
      - CLAUDE.md 负责全局行为说明
      - agents/ 负责子代理定义
      - skills/ 负责工作流与 slash 技能
      - hooks/ 负责运行时拦截和增强

  omcSystemPrompt 和 CLAUDE.md 的关系
  可以这样理解：

  - omcSystemPrompt
      - 偏“库模式 / SDK 模式”的系统提示模板
      - 见 src/agents/definitions.ts:289
  - CLAUDE.md
      - 偏“Claude Code 插件模式”的长期配置入口
      - 安装到用户的 Claude 环境里，被 Claude Code 持续读取

  也就是说，它们表达的是同一套 orchestrator 思想，但落地渠道不同。

  三种模式区分

  - 库模式
      - 用 createOmcSession() + query()
      - 这里 omcSystemPrompt 直接进入 systemPrompt
  - CLI / 插件模式
      - 用 omc setup、plugin、CLAUDE.md、skills、hooks
      - 这里主要不是靠 query() 代码注入
  - team / runtime 模式
      - 运行时再通过 hooks、agents、worker 启动命令继续细化行为

  - 你看到的 query() 注入只是 SDK 使用示例
  - OMC 实际在 Claude Code 里主要是通过 CLAUDE.md + plugin agents + skills + hooks 来注入
    orchestrator 行为的，而不是依赖 src/index.ts 注释里的那段代码。

  - CLAUDE.md 里哪些内容相当于 omcSystemPrompt 的替身
  omc 在委托agent 的时候根据自己 agent定义，如果我的claude 自定义了一下 agent,在自动委托的
  是会 委托我自己定义的agents吗