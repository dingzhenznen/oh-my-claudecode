Explore优先使用的决策依据分析  中讲到 


模型的上下文窗口有限 

explore: 探索 检索使用廉价模型
也会产生大的上下文 使用多agent 隔离

一个agent 你给他东西太多太杂会陷入混乱注意力分散稀释
 上百个工具可能会调不准
所以 用多个agent 使用不同的工具

explore → analyst → planner → architect → executor → verifier


build muti-agent how and when 提到构建agent 不应该以角色 而是应该以上下文